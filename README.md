# Dmx-Network
A DMX sender/receiver module that interfaces with a MAX485 transceiver to handle DMX communication, and bridges this data to an ESP-NOW wireless network. This allows other modules to seamlessly receive DMX data from the network or transmit their own data back through the system.
